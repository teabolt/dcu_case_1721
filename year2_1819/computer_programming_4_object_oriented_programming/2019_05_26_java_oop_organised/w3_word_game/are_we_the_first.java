
// Create a Word class with one static method called isFirstLetter

public class Word {
    
    static boolean isFirstLetter(String word, char letter) {
        return word.charAt(0) == letter;   
    }
}